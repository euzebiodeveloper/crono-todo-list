# Crono Todo List - Frontend

Frontend minimal com Vite + React.

Como rodar:

1. Instale dependências: `npm install` (ou `pnpm`/`yarn`).
2. Rode: `npm run dev`.

Configure `VITE_API_BASE` se o backend estiver em outro endereço.
