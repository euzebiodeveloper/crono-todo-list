# Crono Todo List - Backend

Backend minimal para o Crono Todo List.

Como usar:

1. Copie `.env.example` para `.env` e ajuste `MONGO_URI`.
2. Instale dependências: `npm install`.
3. Rode em desenvolvimento: `npm run dev` (precisa do `nodemon`).
4. API base: `http://localhost:5000/api/todos`.

Stack: Node.js, Express, Mongoose
